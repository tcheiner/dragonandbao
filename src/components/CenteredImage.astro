---
export interface Props {
    src: string;
    alt: string;
}

const { src, alt } = Astro.props;
---

<div class="flex justify-center my-8">
    <img src={src} alt={alt} class="rounded-lg shadow-lg max-w-full" />
</div>