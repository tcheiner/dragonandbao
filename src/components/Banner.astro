---
export interface Props {
    title: string;
    subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<div class="text-center bg-gray-100 py-8 mb-8">
    <h1 class="text-4xl font-bold">{title}</h1>
    <p class="text-lg text-gray-700 mt-2">{subtitle}</p>
</div>